[package]
name = "ibverbs-sys"
version = "0.1.0"
edition = "2021"
description = "Minimal FFI bindings to libibverbs for RDMAS"
license = "MIT OR Apache-2.0"
links = "ibverbs"
build = "build.rs"

[dependencies]
libc = "0.2"

[build-dependencies]
bindgen = "0.70"
pkg-config = "0.3"
cc = "1.0"
